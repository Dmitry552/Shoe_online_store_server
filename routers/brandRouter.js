const Router = require('express');
const router = new Router();

const brandController = require('../controllers/brandController');

//router.post('/registration', comparisonController.registration)

module.exports = router;