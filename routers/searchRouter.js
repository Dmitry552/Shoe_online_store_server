const Router = require('express');
const router = new Router();

const searchController = require('../controllers/searchController');

//router.use('/search', searchController.registration);

module.exports = router;